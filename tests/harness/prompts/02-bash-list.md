<!-- allowed_tools: Bash; exercises: a single Bash tool call followed by a one-line text answer -->

Use `bash` to list `.cs` files under the `src/` directory of this repository, then tell me how many there are. Be terse.
