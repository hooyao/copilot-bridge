<!-- allowed_tools: Bash; exercises: prompt caching across multiple model turns within one Claude Code invocation; verify cache_read_input_tokens > 0 on the 2nd+ upstream-response logs -->

Run `bash` once to print the current working directory, then run `bash` again to list its immediate contents. Output only what bash returned, nothing else.
