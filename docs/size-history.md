# Binary size history

Tracks the size of the published `copilot-bridge` binary after each significant
change, built under Release AOT publish. Lets us catch a runaway dependency early.

How to measure (Windows, PowerShell):

```pwsh
.\aot_publish.bat
(Get-Item .\publish\copilot-bridge.exe).Length / 1KB
```

Cross-platform sizes (linux-x64, osx-arm64, win-arm64) can't be produced on a
Windows dev box — Native AOT doesn't cross-OS compile. They're reported by the CI
`aot-crossplat` job and the release build matrix (see the job summary on each
run). Expect the unix ELF/Mach-O binaries to be in the same ballpark as win-x64.

| Date | Commit / tag | .exe size | Notes |
| --- | --- | --- | --- |
| 2026-05-06 | M0 scaffold | **746 KB** | Just the `Program.cs` placeholder; no dependencies (no `System.*` HTTP, no `Microsoft.AspNetCore.*`). Baseline for all later size analysis. |
| 2026-05-06 | auth subcommand | **4.2 MB** | Added `HttpClient` (`System.Net.Http`, with TLS/sockets), `System.Text.Json` source generator, `System.Security.Cryptography.ProtectedData`, the device-code flow, and the DPAPI `TokenStore`. Delta: +3.5 MB. |
| 2026-05-06 | M1 step 1 + 2 | **4.4 MB** | M1 step 1: `CopilotTokenClient` + `AuthService.GetCopilotTokenAsync` with refresh timer. M1 step 2: Copilot model DTOs, `CopilotHeaderFactory` (the official 7-header set), `CopilotClient.GetModelsAsync`, and the `debug list-models` subcommand. Delta: +200 KB. |
| 2026-05-06 | M1 step 3 (Anthropic DTOs) | **5.25 MB** | Modeled after `@anthropic-ai/sdk` v0.94.0 beta types. ~25 records across `Models/Anthropic/{Common,Request,Response,Stream,Models,CountTokens,Converters}/`. Polymorphic via `[JsonPolymorphic]` + `[JsonDerivedType]` on 7 discriminated unions (content blocks request/response, image source, document source, tool choice, thinking config, context edit, stream event, content block delta). Two custom converters normalize the `string \| Array` unions on `messages[].content` and `system`. AOT clean, source-gen registers all reachable types. Delta: +864 KB — most of which is source-gen `JsonTypeInfo` for the polymorphic variants. |
| 2026-05-06 | M1 step 6a (Kestrel + endpoints + logger) | **9.44 MB** | Added `<FrameworkReference Include="Microsoft.AspNetCore.App" />` for Kestrel + minimal API. Three endpoints: `POST /v1/messages` (byte-passthrough with `[DONE]` filter via `System.Net.ServerSentEvents.SseParser`), `POST /v1/messages/count_tokens` (placeholder `{input_tokens:1}`), `GET /v1/models` (Copilot `/models` filtered to `/v1/messages`-supporting + projected to Anthropic shape). `BridgeRequestLogger` writes `logs/<utc>-<seq>.json` per request — inbound headers/body, upstream URL/headers/body, all SSE events (incl. dropped `[DONE]` with `filtered:true`), duration. `KestrelServer` uses `WebApplication.CreateSlimBuilder()` for AOT, explicit DI factories everywhere. `serve [--port N]` subcommand. Verified end-to-end: `claude -p "Say hello in three words."` → "Hello, let's build." Delta: +4.19 MB — almost all of it is AspNetCore framework. |
| 2026-05-07 | Pipeline framework scaffold | **9.44 MB** | Migration step 1: empty pipeline framework under `src/CopilotBridge.Cli/Pipeline/`. Generic typed `BridgeContext<TBody>` + `BridgeRequest<TBody>` + `BridgeResponse` (streaming OR buffered modes) + `RouteTarget` (vendor + endpoint + model). Interfaces: `IRequestStage<TBody>`, `IResponseStage<TBody>`, `IUpstreamStrategy<TBody>`, `IStrategyRegistry<TBody>`, `IClientInboundAdapter<TClientBody,TIR>`, `IClientOutboundAdapter<TIR>`, `IModelRegistry`. `Pipeline<TBody>` assembly + `PipelineRunner<TBody>` with stage timing. `DiagLog` static class with all methods `[Conditional("BRIDGE_DIAG")]` — populated only in Debug, stripped in Release. `BRIDGE_DIAG` defined in csproj's Debug `PropertyGroup`. Existing endpoints unchanged; framework dormant (no consumers yet). AOT delta: **+2 KB** (essentially noise — unused generic interfaces get trimmed). |
| 2026-05-07 | DiagTracer rework (FsTracer pattern + bounded queue) | **9.44 MB** | Replaced multi-method `DiagLog` with single-surface `DiagTracer.Log(string)` / `DiagTracer.Log(object?)`, both `[Conditional("BRIDGE_DIAG")]`. Output is a separate text file (env var `BRIDGE_DIAG_FILE`) instead of an in-memory section of the audit JSON. Producer side: bounded `Channel<string>` (4096 line capacity) with non-blocking `TryWrite` + drop counter. Consumer side: dedicated `Task.Run` worker that batch-flushes the `StreamWriter` every 64 lines. Removed `BridgeRequestLog.Diag` property + `BridgeRequestLogger.DiagNode`. AOT unchanged (still trimmed since no callers). |
| 2026-05-07 | MVP — pipeline driving `/cc/v1/...` E2E | **9.56 MB** | Identity adapters under `Pipeline/Adapters/ClaudeCode/` + routing via `CopilotModelRegistry` + `ModelRouterStage` + `HeadersOutboundStage` + `CopilotMessagesPassthroughStrategy` + `DoneFilterStage` + assembly in `Hosting/BridgePipelines.cs`. New endpoints `Endpoints/ClaudeCode/{Messages,CountTokens,Models}Endpoint` mounted at `/cc/v1/...`. `KestrelServer` registers pipeline + adapters in DI. `.claude/settings.local.json` switched to `ANTHROPIC_BASE_URL=http://localhost:8765/cc`. Verified: `claude -p "Say hello in three words."` → "Hello, ready to build." Audit log shows `anthropic-beta` upstream filtered to just `context-management-2025-06-27` (Claude Code sent 4 betas; bridge per chatEndpoint.ts:193-210 logic). Old `/v1/...` endpoints kept active for fallback during migration. AOT delta: **+128 KB** (the live framework — Pipeline / Stages / Strategy / Adapters all referenced now). |
| 2026-05-07 | Preprocessing stages full set | **9.57 MB** | Five preprocessing stages under `Pipeline/Stages/Anthropic/`: `SystemSanitizeStage` (strips `# currentDate\nToday's date is YYYY-MM-DD.\n` from system + user message text), `MessagesSanitizeStage` (drops "Tool loaded." text blocks + appends "Please continue." user message when last message is assistant), `AssistantThinkingFilterStage` (drops thinking blocks lacking valid signature), `ThinkingRewriteStage` (haiku→explicit, opus-4.7→adaptive+effort, sonnet→passthrough), `ToolsSanitizeStage` (drops `mcp__ide__executeCode` unless `defer_loading=true`). `CacheControlCleanStage` skipped — DTO design (no `Scope` property on `CacheControl`) silently drops it during deserialize. Assembled in `BridgePipelines` in 7-stage order (router → 4 cleanup → thinking-rewrite → headers-outbound). **Cache hit confirmed empirically: identical-prompt second run reuses all 53,680 prefix tokens (`cache_read_input_tokens=53680`, `cache_creation_input_tokens=0`).** AOT delta: **+9 KB** — stages compile to compact IL. |
| 2026-05-07 | Legacy endpoint cleanup | **9.50 MB** | Migration plan step 9: deleted `Endpoints/{Messages,CountTokens,Models}Endpoint.cs` (the unprefixed `/v1/...` set kept around as fallback) and removed their imports + `Map` calls from `KestrelServer`. The pipeline-driven `/cc/v1/...` endpoints now own all routing. Step 10 regression: playground **24/24 pass** (18 original + 6 BetaAcceptance), harness prompts 01 (simple-text) and 02 (bash-list) both succeed via `claude -p` through the bridge, all 3 captured logs show `0` `currentDate` occurrences in upstream bodies. AOT delta: **-69 KB**. |
| 2026-05-07 | Routing config (first cut) | **9.52 MB** | First-pass routing-config layer: `appsettings.json` ships next to the .exe with `Routing.ShapeRewrites` + `Routing.VariantRoutes` two-phase scheme. New types `Pipeline/Routing/{RoutesConfig,ModelRouteResolver,RoutesValidator}.cs`; `ModelRouterStage` consumes `IOptions<RoutesConfig>`; `ThinkingRewriteStage.cs` deleted (its rules moved to JSON). `KestrelServer` adds `IConfiguration` JSON binding + fail-fast `RoutesValidator` at startup. `BridgeRequestLogger` gains `BRIDGE_VERBOSE_LOG` env var to mirror per-request bodies to stderr. Verified the opus-4.7 + effort:max regression (Copilot 400 "not supported by model claude-opus-4.7") routes correctly to `claude-opus-4.7-xhigh` after the rewrite. AOT delta: **+20 KB** — pure data + a small resolver. |
| 2026-05-08 | Capability layer + Serilog + System.CommandLine | **11.01 MB** | Three independent migrations landed together. (1) **Routing redesign**: `RoutesConfig.VariantRoutes` removed; per-model effort routing folded into `CopilotModelRegistry.EffortAware` (a C# capability dictionary keyed by canonical model id). `appsettings.json` keeps only `Routing.Rules` for user preferences (Match → Rewrite). Resolver: phase 1 walks user rules first-match-wins; phase 2 calls `IModelRegistry.ApplyEffortRouting(model, effort)`. User rule that sets `Effort` claims that field — phase 2 capability is skipped for it (so `{Effort:"xhigh"}` round-trips even when the model isn't in the capability table). (2) **Logger swap**: `Pipeline/DiagTracer.cs` deleted; replaced by Serilog 4.3.1 (PR #2175 makes the destructuring path AOT-clean) with `Sinks.Console` 6.1.1 + `Sinks.File` 7.0.0. All 30+ `DiagTracer.Log(...)` call sites become `Log.Debug(...)`. File sink writes `<exe>/log/bridge-{YYYYMMDD-HHMMSS}.log` (one file per process start, no rolling). Console sink mirrors to stderr from level Verbose+. `BRIDGE_LOG_LEVEL` env var (default Debug) controls minimum level. (3) **CLI**: `Program.cs` rewritten with `System.CommandLine` 3.0.0-preview.3 — typed options (`--port`, `--all`), nested commands (`auth login/whoami/status/copilot-status/logout`, `debug list-models`), auto-generated `--help` / `--version`. `ServeCommand.RunAsync(int)` / `DebugCommand.ListModelsAsync(bool)` / `AuthCommand.{LoginAsync,WhoAmIAsync,...}` are typed entry points; the hand-rolled string[] parsing removed. Stable patches alongside: ProtectedData 10.0.0 → 10.0.7, Test.Sdk 17.14.1 → 18.5.1, xunit.runner.visualstudio 3.1.4 → 3.1.5. AOT publish 0/0 (Serilog 4.3.0+ closes the IL2072 trim warning). AOT delta: **+1.49 MB** ≈ +16% — almost entirely Serilog (~700 KB) + System.CommandLine (~700 KB) + new sinks. |
| 2026-06-06 | M2 cross-platform (win-x64) | **11.30 MB** | Cross-platform token storage: `TokenStore` split into a runtime-dispatching facade over `ITokenProtector` — `WindowsDpapiTokenProtector` (DPAPI, unchanged on Windows) vs `DerivedKeyTokenProtector` (HKDF-SHA256 → AES-256-CBC + HMAC-SHA256, key from `MachineKeyProvider`) on Linux/macOS. Removed the assembly-wide `[SupportedOSPlatform("windows")]`; the DPAPI type is now the only Windows-attributed surface (CA1416 satisfied via `OperatingSystem.IsWindows()` guard). New hidden `debug selftest-tokenstore` command for CI smoke. win-x64 only — measured locally (`11,850,752` bytes). linux-x64 / osx-arm64 / win-arm64 sizes are reported by CI (can't build cross-OS locally). AOT delta: **+0.29 MB** — the HKDF/AES/HMAC primitives are all in the BCL, so this is mostly the new protector/provider types. |
| 2026-06-06 | M2 cross-platform (other RIDs, from CI) | — | First cross-OS AOT publishes, measured on their own GitHub runners (run `27051643607`): **win-arm64 11.75 MB** (12,318,208 B), **osx-arm64 12.38 MB** (12,983,424 B), **linux-x64 13.09 MB** (13,727,352 B). Same source as the win-x64 row; size differs by target runtime/linker. All three passed the `debug selftest-tokenstore` round-trip on real hardware (DPAPI on win-arm64; machine-derived AES-CBC+HMAC on linux/osx). |
| 2026-06-15 | freeze-ir-provider-extensions (win-x64) | **11.327 MB** | Codex-support change 1/3: froze the IR (Anthropic-shape DTOs) and added the `ProviderExtensions` namespaced escape-hatch (`Models/Common/ProviderExtensions.cs`, a `Dictionary<string,JsonElement>` bag) at request + content-part level, registered in `JsonContext`. AOT publish **0 warnings** — the `JsonElement` bag is source-gen-clean, no IL2026/IL3050. win-x64 measured locally (`11,877,376` B). AOT delta: **+26 KB** over the 2026-06-06 win-x64 row (`11,850,752` B) — just the source-gen `JsonTypeInfo` for the new `ProviderExtensions` type. The bag is inert (null) on the Claude Code hot path → `/cc` upstream bytes are byte-identical (H1 gate). |
| 2026-06-15 | add-codex-responses-client (win-x64) | **11.769 MB** | Codex-support change 3/3: the `/codex/responses` endpoint + the four T1–T4 translators routing Codex through the shared Anthropic IR + the fully-typed `Models/Responses/` DTOs (ResponsesRequest + input items + content parts + SSE events) + `CodexModelProfileCatalog` + `CopilotResponsesStrategy`. AOT publish **0 warnings** — all Responses DTOs source-gen, the streaming state machines write JSON via `Utf8JsonWriter` (no reflection serialize), tools held as opaque `JsonElement` (the `[JsonExtensionData]`-on-record-ctor trap avoided). win-x64 measured locally (`12,340,736` B). AOT delta: **+442 KB** over change 1 — the Responses DTO source-gen `JsonTypeInfo` + the four translators + endpoint. `/cc` hot path still byte-identical (H1). |
| 2026-06-16 | Codex PR review fixes + full coverage (win-x64) | **11.795 MB** | Addressed the three-agent PR review: streaming honesty (response.failed → honest terminal; mid-stream throw always flushes a terminal; usage threaded through with `total_tokens` — a missing-field that made `codex.exe` reconnect-loop, caught by re-running the live E1 tool turn), request fidelity (malformed tool args → 400 `CodexBadRequestException`; max_output_tokens round-trip; reasoning.summary re-emit), pipeline gating (`CopilotAnthropicOnlyStage` so Anthropic-only stages no-op on the Codex IR; profile-drift validation), and endpoint audit of the real wire body. Coverage 200 → **254** CI-safe tests (endpoint error mapping, multi-block/multi-tool streams, image/vision, real tool-flow A5b). AOT **0 warnings**; win-x64 `12,368,384` B. `/cc` byte-identical (H1/H2 green). |
| 2026-07-06 | add-client-config-command (win-x64) | **12.16 MB** | The `config` command family (`config claude-code --scope global\|repo`, `config codex`, `config status`) that auto-configures each client to point at the bridge. New `Hosting/ClientConfig/` (isolated web-host-free composition root + `IClientConfigurator` seam + Plan/Apply/Read; JSON merge via `JsonNode`, TOML merge via **Tomlyn `DocumentSyntax`** trivia-preserving syntax tree). New dep **Tomlyn 2.10.1**. win-x64 measured locally (`12,755,968` B). AOT publish **0 warnings**. Size path: referenced-but-unused Tomlyn `12,514,816` B → in-use (DocumentSyntax reachable) `12,755,968` B, so Tomlyn's real footprint is **+235 KB** (pure syntax-tree path, reflection serialization auto-disabled under `PublishAot`). Total **+388 KB** over the 2026-06-16 row also carries unrelated intervening commits (tracing refactor, tool-input-validation detector, gpt-5.5 routing fixes). `serve` runtime untouched — the config command boots no web host. |
| 2026-07-13 | fix-cc-responses-stream-fault (win-x64) | **12.29 MB** | Downstream-owned Responses stream-fault framing, bounded typed `response.failed`, buffered T3/T4 fallback translation before response detectors, fail-closed malformed-response handling, and client config recovery correction. Final reviewed win-x64 source measured locally (`12,888,576` B), **+132,608 B / 1.04%** over the 2026-07-06 baseline. Native AOT publish **0 warnings**; the Release binary contains no Debug-only deterministic-upstream override strings. |
| 2026-07-13 | add-startup-auto-update (win-x64) | **13.12 MB** | Startup self-update: a serve-only synchronous update gate + a second Native AOT executable `copilot-updater`. New `src/Shared.Update/` (frozen cross-executable wire contract with explicit `[JsonPropertyName]` + its own source-gen context, path confinement, secure download, framework-only ZIP/TAR.GZ extraction, config snapshot + template merge, named-pipe IPC with three role-scoped 256-bit capabilities, install/backup/cutover/rollback transaction, process-identity checks, install lock, phase journal) compiled into BOTH the bridge and the updater. Bridge-side: `StartupUpdateGate` (anonymous bounded GitHub Releases discovery, SemVer/channel selection, RID asset selection, consent prompt, plan handoff) + an inert-by-default `UpdateReadinessReporter` hosted service. Bridge win-x64 measured locally (`13,123,072` B) — AOT delta **+367 KB** over the 2026-07-06 row, the source-gen wire `JsonTypeInfo` + GitHub release DTOs + the update/gate/reporter code (no new NuGet dependency; named pipes, ZIP, GZip/TAR, SHA-256 are all BCL). The separate **`copilot-updater` win-x64 is 4.60 MB** (`4,827,136` B) — a plain console app, no ASP.NET framework reference. AOT publish **0 warnings** for both. `serve` hot path unchanged when `AutoUpdate.EnableAutoUpdate=false` or on a `*-dev` build (the gate short-circuits before any network call). linux-x64 / win-arm64 / osx-arm64 reported by CI. |
| 2026-07-27 | bridge-owns-timeouts + IHttpClientFactory (win-x64) | **12.71 MiB (13.33 MB)** | Long-thinking timeout ownership (client env keys derived from the bridge's budgets, coarse `HttpClient.Timeout` removed, per-phase startup report) plus the DI change that motivated the size question: the hand-rolled singleton `HttpClient` replaced by **`IHttpClientFactory`** with four named clients (`copilot-anthropic`, `copilot-responses`, `copilot-metadata`, `github-auth`), so each upstream surface gets its own connection pool and consumers lease per call instead of pinning one pooled handler. Measured locally: `13,328,896` B vs the 2026-07-13 `add-startup-auto-update` row (`13,123,072` B) → **+203,776 B / +199 KB / +1.55%**. That delta is an **aggregate upper bound, not the factory's cost**: eleven unrelated PRs (#44–#53 — codex `custom_tool_call` id round-trip, Copilot web-search relay, the 1M-context config fix, the macOS file-watcher fix, the opus-5 model refresh) landed between the two builds, and the factory's own share was not isolated. Isolating it would need a third publish of this same revision with only the DI change reverted; that was not done, so treat 199 KB as the ceiling on what the switch could have cost. `Microsoft.Extensions.Http` ships in the ASP.NET shared framework, so this is reachable-code cost only — no new NuGet package. AOT publish **0 warnings**. This measurement is what `design.md` §2.2 cites when amending the ban: the ban predated the multi-surface pool requirement, and at most 199 KB buys pool isolation (a Codex burst can no longer starve Claude Code) plus handler rotation. |
| 2026-08-05 | add-codex-model-catalog (win-x64) | **13.281 MiB (13.93 MB)** | Codex-native `GET /codex/models`, the complete embedded Codex 0.144.x catalog baseline, live Copilot limit projection/cache, command-backed discovery auth, and its default-on `appsettings.json` switch. Final reviewed source measured locally through `build-aot.bat`: `13,926,400` B vs the 2026-07-27 row (`13,328,896` B) → **+597,504 B / +583.5 KiB / +4.48%**. Native AOT publish completed with **0 trimming/AOT warnings**. This measurement is informational only: the change has no binary-size acceptance limit, and complete instruction-bearing Codex entries are retained for protocol fidelity. |
| 2026-08-05 | fix-cc-responses-context-accounting (win-x64) | **13.349 MiB (14.00 MB)** | Shared messages/count route planner, strict AOT-safe cross-routed count parsing, exact shared-T2 count body, GPT-5.6 admission calibration, narrow Responses-context-400 rewrite, stable per-response Anthropic message ids, and deterministic Claude compact/retry coverage. Final source after PR review, rebased onto `add-codex-model-catalog`, measured locally with `build-aot.bat`: `13,997,568` B, **+71,168 B / +69.5 KiB / +0.51%** over that change's `13,926,400` B baseline. Native AOT publish emitted **0 warnings**. A live Release/AOT launch with `COPILOT_BRIDGE_TEST_UPSTREAM_BASE_URL=http://127.0.0.1:18766` returned 200 through real Copilot while the test listener observed **0 connections**, proving the Debug-only override was compiled out. |
| 2026-08-05 | make-codex-catalog-version-independent (win-x64) | **13.145 MiB (13.78 MB)** | Replaced the embedded 0.144 catalog with exact-version official-source resolution plus validated memory/disk/GitHub caching, ETag revalidation, atomic persistence, retention, and stale-if-error fallback. The isolated feature revision, measured before rebasing onto `fix-cc-responses-context-accounting`, used the verified VS x64 environment with warnings promoted to errors: bridge `13,783,552` B vs the preceding catalog row (`13,926,400` B) → **-142,848 B / -139.5 KiB / -1.03%**. The separately published updater is `5,019,136` B. Both Native AOT publishes completed with **0 trimming/AOT warnings**; release smoke passed and the package contains `copilot-bridge.exe`, `copilot-updater.exe`, and `appsettings.json`. |
| 2026-08-05 | HybridCache process-memory layer (win-x64) | **13.313 MiB (13.96 MB)** | Replaced the catalog's private memory/in-flight dictionaries with `Microsoft.Extensions.Caching.Hybrid` 10.8.0 while preserving the validated file cache and process-wide disk-writer lock. Clean independent-intermediate Native AOT publish with all warnings promoted to errors: bridge `13,960,192` B vs the pre-HybridCache row (`13,783,552` B) → **+176,640 B / +172.5 KiB / +1.28%**. The package's unused reflection JSON fallback emits three `IL2026`, three `IL3050`, and three `IL2070` diagnostics; the AOT input substitutes throwing bodies only for those four unreachable fallback members, with package-version and assembly-SHA guards. No warning IDs are disabled, and a negative mutation probe confirmed unrelated `IL2026`/`IL3050` diagnostics still fail the publish. The updater remains `5,019,136` B; both publishes completed with **0 trimming/AOT warnings**. |
| 2026-08-06 | fix-codex-response-fidelity (win-x64) | **13.442 MiB (14.09 MB)** | Native Codex response fidelity carrier/ledger: every clean `/responses` SSE event now survives at full JSON value fidelity after semantic IR inspection, while detector mutations remain authoritative and partial faults end with one honest failed terminal. Also corrects tool-result `input_text`/`output_text` flattening and reasoning-summary echo loss found by corpus/live-client replay, while retaining the required `ctc` custom-tool identity correction. Final source measured locally through the verified VS x64 environment: bridge `14,094,848` B vs the current main baseline (`13,960,192` B) → **+134,656 B / +131.5 KiB / +0.96%**. The ledger is allocated only on native `/codex` streaming routes; ordinary streaming holds at most one source event, block buffering retains one text/thinking block, and the `/cc` hot path remains allocation-free for this feature. The updater remains `5,019,136` B. Both Native AOT publishes completed with **0 trimming/AOT warnings**. |
| 2026-08-06 | fix-cc-responses-multimodal-tool-results (win-x64) | **13.447 MiB (14.10 MB)** | Preserves Claude Code image-bearing tool results as Responses `input_text`/`input_image` content items on the exact live-proven `gpt-5.6-sol` profile, with explicit client-protocol provenance, whole-array compatibility fallback, and vision signaling. Clean Release/RID build followed by both Native AOT publishes in the verified VS x64 environment: bridge `14,099,968` B (**+5,120 B / +5.0 KiB / +0.04%** over the preceding row), updater unchanged at `5,019,136` B; both mtimes advanced and both publishes emitted **0 trimming/AOT warnings**. |
| 2026-08-06 | fix-cc-responses-reasoning-replay (win-x64) | **13.457 MiB (14.11 MB)** | Carries Responses reasoning state across the Claude Code edge and restores it on the tool-result turn, and — after review — moves BOTH this and the multimodal transform onto the source-push/dest-pull layering the IR requires: translators no longer take a client-identity parameter, the source states facts on the IR (`bridge_reasoning_item`, `opaque_tool_output`) and each dest pulls what its own protocol needs. Clean Release/RID rebuild with both Native AOT publishes in the verified VS x64 environment: bridge `14,111,232` B (**+11,264 B / +11.0 KiB / +0.08%** over the preceding row), updater unchanged at `5,019,136` B; both mtimes advanced and both publishes emitted **0 trimming/AOT warnings**. |
| 2026-08-06 | harden-reasoning-carrier-compat (win-x64) | **13.474 MiB (14.13 MB)** | Makes the reasoning carrier survive version skew and model re-routing, the two ways durable client state fails quietly. The version moves OUT of the discriminator (`cbridge_rr_7f3a9d2c:` + a separately parsed version), so a carrier from a newer build — or replayed into a rolled-back build — is recognised as bridge-owned and fails closed with a bounded 400 instead of being forwarded to Copilot as provider-native encrypted content; the legacy `_v1:` prefix stays readable and is still what this release EMITS, because a writer must never ship before a reader. T3 also stamps the producing model (`bridge_reasoning_origin`) so a carrier replayed after a model change is dropped rather than handed to a model that cannot read it, and the multimodal capability becomes three-valued so an uncatalogued model downgrading an image is logged instead of silently sending text. Clean Release/RID build with the Native AOT publish in the verified VS x64 environment: bridge `14,128,128` B (**+16,896 B / +16.5 KiB / +0.12%** over the preceding row), updater unchanged at `5,019,136` B; mtime advanced and the publish emitted **0 trimming/AOT warnings**. |
| 2026-08-07 | fix-codex-request-fidelity (win-x64) | **13.896 MiB (14.57 MB)** | Preserves native Codex request JSON values/order through the shared IR via source-pushed, destination-pulled provider extensions at request/message/part level: `reasoning.context` (including explicit null), developer positions, valid message/tool identities, future siblings, complete reasoning state, and native structured function output. Destination-owned invalid `item_0` removal and profile coercions remain narrow and observable; Claude→Responses stays source-agnostic. Same-machine detached `ed8633e` baseline `14,516,224` B → final bridge `14,570,496` B (**+54,272 B / +53.0 KiB / +0.37%**). Fresh updater `5,019,136` B. Both Native AOT publishes completed with **0 trimming/AOT warnings**. |
| 2026-08-11 | fix-auth-token-lifecycle (win-x64) | **14.011 MiB (14.69 MB)** | Adds the encrypted versioned GitHub OAuth credential/mirror, rotating proactive/reactive refresh with path-locked atomic commits, receipt-relative immutable Copilot auth leases, one bounded byte-identical CAPI 401 replay, typed secret-free diagnostics, and contract/behavior seams. Final review also makes an anomalous refresh response drop the spent prior refresh token instead of persisting it, distinguishes fresh logins by credential id plus generation, separates transient refresh outages from credential rejection, serializes fresh-login/logout writers with refresh locks, and moves the bootstrap→full Serilog swap early enough that startup/auth lifecycle events reach the rolling log. Final bridge `14,691,840` B vs the preceding `14,570,496` B → **+121,344 B / +118.5 KiB / +0.83%**; updater unchanged at `5,019,136` B. `build-aot.bat` plus a fresh updater publish both completed with **0 trimming/AOT warnings**, package contains bridge/updater/appsettings, and the AOT binary contains no DEBUG auth-seam strings. Real Codex 0.147.0-alpha.6.6 app-server gpt-5.6 tool execution crossed a genuine rejected bearer, refreshed/replayed once, completed three tool calls, and had zero client router fatals. |
| 2026-08-14 | recover-copilot-bearer-on-forbidden (win-x64) | **14.072 MiB (14.76 MB)** | Extends the generation-aware exact CAPI auth replay from 401 to an ambiguous first 403, adds status-specific terminal classification and a configurable live-overlay failure cooldown, and adds forced-403 real-client evidence. Final bridge `14,755,328` B vs the pre-change local artifact `14,699,520` B → **+55,808 B / +54.5 KiB / +0.38%**; updater remains `5,019,136` B. Fresh bridge/updater Native AOT publishes completed with 0 trimming/AOT warnings, the package contains both executables plus `appsettings.json`, the bridge `--version` smoke passed, and the release-native binary contains no Debug seam strings. |
| 2026-08-24 | add-built-in-github-cli-oauth (win-x64) | **14.114 MiB (14.80 MB)** | Introduces CredentialService as the sole credential owner and one exe-local `github_credentials.dat`: semantic version 1 transactionally migrates complete `github_credentials.v2.dat` state or raw `github_token.dat`, verifies the new file, then deletes both old files; a still-working version 1 retains exchange/refresh until true terminal rejection, and the next explicit built-in GitHub CLI OAuth login atomically writes direct-CAPI version 2 without `gh` or a client secret. Final bridge `14,799,360` B vs `14,755,328` B → **+44,032 B / +43.0 KiB / +0.30%**; updater remains `5,019,136` B. Fresh bridge Native AOT completed with 0 trimming/AOT warnings; updater was unchanged/up-to-date; the release binary contains neither the GitHub CLI web-flow secret nor Debug auth-seam strings. Real Codex app-server `0.148.0-alpha.15` passed migrated-version-1 exchange before that source token was later evicted, and the final version-2 direct run also exercised residual legacy cleanup; both completed matching custom-tool call/output loops, returned their canaries, and recorded zero router fatals, ERROR rows, or retries. |
